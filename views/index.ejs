<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Animal Rescue Site</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: #0a0a0a;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
        }

        .header {
            position: absolute;
            top: 30px;
            text-align: center;
            width: 100%;
            color: #fff;
            z-index: 1;
        }

        .header h1 {
            font-size: 3em;
            font-weight: 700;
            text-shadow: 0 2px 20px rgba(255, 255, 255, 0.3);
            letter-spacing: 2px;
        }

        .container {
            position: relative;
            width: 900px;
            height: 700px;
            display: grid;
            grid-template-columns: 1fr 1fr;
            grid-template-rows: 1fr 1fr;
            gap: 40px;
            padding: 20px;
        }

        .box {
            background: linear-gradient(145deg, #ff9500, #ff7300);
            border-radius: 30px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            font-size: 2em;
            font-weight: 600;
            color: white;
            cursor: pointer;
            transition: all 0.4s ease;
            box-shadow: 0 10px 30px rgba(255, 149, 0, 0.4);
            text-align: center;
            padding: 30px;
            position: relative;
            z-index: 1;
        }

        .box:hover {
            transform: scale(1.05);
            box-shadow: 0 15px 40px rgba(255, 149, 0, 0.6);
        }

        .box-icon {
            font-size: 3em;
            margin-bottom: 15px;
        }

        .emergency-btn {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 350px;
            height: 350px;
            background: linear-gradient(145deg, #ff2020, #cc0000);
            border-radius: 50%;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            font-size: 2.2em;
            font-weight: 700;
            color: white;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 0 40px rgba(255, 32, 32, 0.7), 0 0 80px rgba(255, 32, 32, 0.4);
            text-align: center;
            z-index: 10;
            animation: pulse 2s infinite;
        }

          .outer-emergency-btn {
            position: absolute;
            top: 50%;
            left: 50%;
             transform: translate(-50%, -50%);
            width: 450px;
            height: 450px;
            background: black;
            border-radius: 50%;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            z-index: 9;

        }

            
        .tagline {
            position: absolute;
            /* bottom: 60px; */
            text-align: center;
            width: 100%;
            color: #fff;
            font-size: 1.5em;
            font-weight: 500;
            z-index: 1;
            text-shadow: 0 2px 10px rgba(255, 255, 255, 0.3);
            opacity: 0.9;
        }

        @keyframes pulse {
            0%, 100% {
                box-shadow: 0 0 40px rgba(255, 32, 32, 0.7), 0 0 80px rgba(255, 32, 32, 0.4);
            }
            50% {
                box-shadow: 0 0 60px rgba(255, 32, 32, 0.9), 0 0 100px rgba(255, 32, 32, 0.6);
            }
        }

        .emergency-btn:hover {
            transform: translate(-50%, -50%) scale(1.1);
            box-shadow: 0 0 70px rgba(255, 32, 32, 1), 0 0 120px rgba(255, 32, 32, 0.7);
            animation: none;
        }

        .emergency-icon {
            font-size: 1.8em;
            margin-bottom: 15px;
            animation: flash 1s infinite;
        }

        @keyframes flash {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.6; }
        }

        @media (max-width: 1024px) {
            .container {
                width: 700px;
                height: 550px;
                gap: 30px;
            }

            .box {
                font-size: 1.5em;
                border-radius: 25px;
            }

            .box-icon {
                font-size: 2.5em;
            }

            .emergency-btn {
                width: 280px;
                height: 280px;
                font-size: 1.8em;
            }

            .header h1 {
                font-size: 2.5em;
            }
        }

        @media (max-width: 768px) {
            .container {
                width: 90vw;
                height: 90vw;
                max-width: 500px;
                max-height: 500px;
                gap: 20px;
            }

            .box {
                font-size: 1.2em;
                border-radius: 20px;
                padding: 20px;
            }

            .box-icon {
                font-size: 2em;
            }

            .emergency-btn {
                width: 200px;
                height: 200px;
                font-size: 1.3em;
            }

            .emergency-icon {
                font-size: 1.5em;
            }

            .header h1 {
                font-size: 1.8em;
                top: 15px;
            }
        }

        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.95);
            justify-content: center;
            align-items: center;
            z-index: 1000;
        }

        .modal-content {
            background: linear-gradient(145deg, #2a2a2a, #1a1a1a);
            padding: 50px;
            border-radius: 25px;
            max-width: 550px;
            width: 90%;
            color: white;
            text-align: center;
            box-shadow: 0 0 60px rgba(255, 32, 32, 0.6);
            border: 2px solid #ff2020;
        }

        .modal-content h2 {
            color: #ff2020;
            margin-bottom: 25px;
            font-size: 2.5em;
        }

        .modal-content p {
            margin-bottom: 20px;
            line-height: 1.8;
            font-size: 1.1em;
        }

        .modal-content strong {
            color: #ff2020;
            font-size: 1.3em;
        }

        .close-btn {
            background: linear-gradient(145deg, #ff2020, #cc0000);
            color: white;
            border: none;
            padding: 15px 40px;
            border-radius: 30px;
            cursor: pointer;
            font-size: 1.1em;
            font-weight: 600;
            transition: all 0.3s ease;
            margin-top: 10px;
        }

        .close-btn:hover {
            background: linear-gradient(145deg, #cc0000, #aa0000);
            transform: scale(1.05);
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>🐾 Animal Rescue Hub 🐾</h1>
    </div>

    <div class="container">
        <div class="box">
            <a href="/petshop"><div class="box-icon">🏪</div>
            Pet Shop
            </a>
        </div>
        
        <div class="box" >
          <a href="/about"><div class="box-icon">🏪</div>  <div class="box-icon">🦁</div>
            About Animals
             </a>
        </div>
        <div class="box">
          <a href="/discussion_section"><div class="box-icon">🏪</div>  <div class="box-icon">💬</div>
            Share Experience
             </a>
        </div>
        <div class="box">
           <a href="/nearby_NGOs"><div class="box-icon">🏪</div> <div class="box-icon">🏥</div>
            Nearby NGO's
             </a>
        </div>

        <div class="emergency-btn" onclick="showEmergency()">
            <div class="emergency-icon">🚨</div>
            Emergency<br>Request
        </div>
        <div class="outer-emergency-btn" >
    
        </div>
    </div>

<p class="tagline">Saving lives, one paw at a time 🐶🐾</p>


    <div id="emergencyModal" class="modal">
        <div class="modal-content">
            <h2>🚨 Emergency Alert!</h2>
            <p>This will connect you to emergency animal rescue services immediately.</p>
            <p><strong>Emergency Hotline: 1-800-RESCUE</strong></p>
            <p>Stay calm. Help is on the way!</p>
            <button class="close-btn" onclick="closeModal()">Close</button>
        </div>
    </div>

    <script>
        function navigateTo(page) {
            alert('Navigating to: ' + page.replace(/-/g, ' ').toUpperCase());
        }

        function showEmergency() {
            document.getElementById('emergencyModal').style.display = 'flex';
        }

        function closeModal() {
            document.getElementById('emergencyModal').style.display = 'none';
        }

        window.onclick = function(event) {
            const modal = document.getElementById('emergencyModal');
            if (event.target === modal) {
                modal.style.display = 'none';
            }
        }
    </script>
</body>
</html> -->








<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <style>
        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body{
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: #0a0a0a;
            height: 100vh;
            width: 100%;
        }

        #head{
            color: white;
            font-size: 50px;
            text-align: center;

        }

        #container{
            width: 100%;
            height: 90vh;
            margin: auto;
            margin-top: 50px;
            display: grid;
            grid-template-columns: 1fr 1fr;
            grid-template-rows: 1fr 1fr;
            gap: 30px;
            position: relative;
            justify-items: center;  
        }

        .box{
            width: 95%;
            background: linear-gradient(145deg, #ff9500, #ff7300);
            border-radius: 30px;
            display: flex;
            justify-content: center;
            align-items: center;
            font-size: 30px;
            font-weight: 600;
            color: white;
            cursor: pointer;
            transition: all 0.4s ease;
            box-shadow: 0 10px 30px rgba(255, 149, 0, 0.4);
        }

        .box:hover{
            transform: scale(1.05);
            box-shadow: 0 15px 40px rgba(255, 149, 0, 0.6);
        }

        .circle{
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 400px;
            height: 400px;
            background: linear-gradient(145deg, #ff2020, #cc0000);
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            font-size: 24px;
            font-weight: 700;
            color: white;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 0 40px rgba(255, 32, 32, 0.7), 0 0 80px rgba(255, 32, 32, 0.4);
            text-align: center;
            z-index: 10;
            animation: pulse 2s infinite;
        }

        .outer-circle{
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 500px;
            height: 500px;
            background: black;
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 9;

        }

        @keyframes pulse {
            0%, 100% {
                box-shadow: 0 0 40px rgba(255, 32, 32, 0.7), 0 0 80px rgba(255, 32, 32, 0.4);
            }
            50% {
                box-shadow: 0 0 60px rgba(255, 32, 32, 0.9), 0 0 100px rgba(255, 32, 32, 0.6);
            }
        }

        .circle:hover{
            transform: translate(-50%, -50%) scale(1.1);
            box-shadow: 0 0 70px rgba(255, 32, 32, 1), 0 0 120px rgba(255, 32, 32, 0.7);
            animation: none;
        }

        a{
            text-decoration: none;
            color: white;
        }

        .emergency-icon{
            font-size: 2.5em;
            margin-bottom: 15px;
            animation: flash 1s infinite;
        }

        @keyframes flash {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.6; }
        }

                .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.95);
            justify-content: center;
            align-items: center;
            z-index: 1000;
        }

        .modal-content {
            background: linear-gradient(145deg, #2a2a2a, #1a1a1a);
            padding: 50px;
            border-radius: 25px;
            max-width: 550px;
            width: 90%;
            color: white;
            text-align: center;
            box-shadow: 0 0 60px rgba(255, 32, 32, 0.6);
            border: 2px solid #ff2020;
        }

        .modal-content h2 {
            color: #ff2020;
            margin-bottom: 25px;
            font-size: 2.5em;
        }

        .modal-content p {
            margin-bottom: 20px;
            line-height: 1.8;
            font-size: 1.1em;
        }

        .modal-content strong {
            color: #ff2020;
            font-size: 1.3em;
        }

        .close-btn {
            background: linear-gradient(145deg, #ff2020, #cc0000);
            color: white;
            border: none;
            padding: 15px 40px;
            border-radius: 30px;
            cursor: pointer;
            font-size: 1.1em;
            font-weight: 600;
            transition: all 0.3s ease;
            margin-top: 10px;
        }

        .close-btn:hover {
            background: linear-gradient(145deg, #cc0000, #aa0000);
            transform: scale(1.05);
        }
    </style>
</head>
<body>
    <h2 id="head">PashuMitra - The animal rescue site</h2>

<div id="container">
    <!-- Box 1: Adopt / Pet Shop -->
   
   <a href="/petshop" class="box">
    <div class="firbox">
             🏪 <strong>Adopt / Pet Shop</strong>
    </div>
    </a>

    <!-- Box 2: About Animals -->
     <a href="/about" class="box">
    <div class="secbox">
            🐾 <strong>About Animals</strong>
    </div>
      </a>

    <!-- Box 3: Share Experience -->
     <a href="/discussion_section" class="box">
    <div class="thdbox">
            ✍️ <strong>Share Experience</strong>
    </div>
    </a>

    <!-- Box 4: Nearby NGOs -->
    <a href="/nearby_NGOs" class="box">
    <div class="fourbox">
            🏥 <strong>Nearby NGOs</strong>
    </div>
      </a>

    <!-- Center Circle: Emergency Request -->
    <div class="circle emergency-btn" onclick="showEmergency()">
     <div class="emergency-icon">🚨</div>
         <strong>Emergency Request</strong>
    </div>

    <!-- Outer Circle for animation -->
    <div class="outer-circle emergency-btn"></div>      


 <div id="emergencyModal" class="modal">
        <div class="modal-content">
            <h2>🚨 Emergency Alert!</h2>
            <p>This will connect you to emergency animal rescue services immediately.</p>
            <p><strong>Emergency Hotline: 1-800-RESCUE</strong></p>
            <p>Stay calm. Help is on the way!</p>
            <button class="close-btn" onclick="closeModal()">Close</button>
        </div>
    </div>

    </div>

 <script>
        function navigateTo(page) {
            alert('Navigating to: ' + page.replace(/-/g, ' ').toUpperCase());
        }

        function showEmergency() {
            document.getElementById('emergencyModal').style.display = 'flex';
        }

        function closeModal() {
            document.getElementById('emergencyModal').style.display = 'none';
        }

        window.onclick = function(event) {
            const modal = document.getElementById('emergencyModal');
            if (event.target === modal) {
                modal.style.display = 'none';
            }
        }
    </script>
    
</body>
</html>